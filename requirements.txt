# Deps for the specforge tool itself (generator/renderer/executor/analysis, Phase 2+).
# target_api is a separate deployable with its own pinned requirements.txt and
# its own CI job — its deps deliberately do not appear here.
anthropic>=0.69.0
pydantic>=2.9.0
python-dotenv>=1.0.0
httpx>=0.27.0
pytest>=8.0.0
