# Deps for the specforge tool itself (generator/renderer/executor/analysis, Phase 2+).
# target_api has its own pinned requirements.txt and its own CI job.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
sqlalchemy>=2.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9
python-dotenv>=1.0.0
anthropic>=0.40.0
httpx>=0.27.0
pytest>=8.0.0